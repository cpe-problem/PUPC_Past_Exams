#include <bits/stdc++.h>

using namespace std;

int main()
{
    int num;
    cin >> num;
    string line;
    getline(cin, line);
    while(num--)
    {
        string g1, v1, g2, v2;
        getline(cin, g1);
        getline(cin, v1);
        getline(cin, g2);
        getline(cin, v2);
    }
}